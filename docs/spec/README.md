# spec

Normative. Everything here defines bytes, and changing bytes is a breaking
change.

- [`signature.md`](signature.md)  the first-order signature: sorts, operations
- [`canonical.md`](canonical.md)  the ATerm format, derived empirically from
  real Nix, with the probe that established each rule
- [`store-paths.md`](store-paths.md)  how a store path is computed, verified
  against real Nix: 1259 of 1259 output paths across 805 real derivations
- [`examples/`](examples/)        golden `.drv` files generated by real Nix

The rule that governs this directory comes from
[`../theory.md`](../theory.md) section 4: reproducibility is the
well-definedness of a quotient, and it holds only if normalization is confluent
and terminating. So canonical serialization is not a formatting preference, it
is the proof obligation. If two implementations can disagree on bytes for the
same intent, this directory has a bug, regardless of what the implementations
do.

## Licence

**This directory is CC0-1.0**, unlike the rest of the repository, which is
MPL-2.0.

A specification exists to be implemented, including by people who owe this
project nothing. Nobody should have to reason about whether reading it
encumbers their code, so the answer is made trivial: it does not. Implement it
in anything, under any licence, with or without attribution.

See [`../decisions/2026-08-01-licence-mpl-2.0.md`](../decisions/2026-08-01-licence-mpl-2.0.md).
